<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HappyPlaceApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HappyPlaceApp" parent="Base.Theme.HappyPlaceApp" />
    
    <style name="CustomNoActionBarTheme" parent="Base.Theme.HappyPlaceApp">
        <item name="android:statusBarColor">@color/primaryDark</item>
    </style>

    <style name="CustomToolBarStyle">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:colorControlNormal">@color/white</item>
    </style>
</resources>